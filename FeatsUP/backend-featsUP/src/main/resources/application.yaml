ktor:
  deployment:
    port: 8081

  application:
    modules:
      - com.featup.ApplicationKt.module

database:
  driver: org.postgresql.Driver
  url: "jdbc:postgresql://localhost:5432/featsup"
  user: "postgres"
  password: "nesantari"

jwt:
  secret: "${JWT_SECRET}"   # Aquí toma el valor de tu env var
  issuer: "featup"
  audience: "featup_audience"
  realm: "featup-realm"
  expiresInMinutes: 60
